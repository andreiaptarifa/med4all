<%# <div class="container py-5 mt-5">
  <div class="row">
    <div class="col-12">
      <div class="treatment_container">
      <h1>Suas doações:</h1>
      <table class="table table-striped justify-content-center">
        <thead>
          <tr>
            <th>Medicamento</th>
            <th>Principio ativo</th>
            <th>Concentração</th>
            <th>Quantidade</th>
            <th>Data da doação</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
        <% @donations.each do |donation| %>
          <%# <tr>
            <td><%= donation.medication.commercial_name %>
            <%# <td><%= donation.medication.active_substance %>
            <%# <td><%= donation.medication.concentration %>
            <%# <td><%= donation.units %>
            <%# <td><%= donation.created_at.strftime("%d-%m-%y") %>
          <%# </tr>
        <% end %>
        <%# </tbody>
      </table>
      <h2>Obrigado, sua doação ajuda a salvar vidas!</h2>
      </div>
   </div>
  </div>
</div> %>
<%#
<div class="d-flex justify-content-center container m-5">
  <%= link_to 'Nova doação', new_donation_path, class: 'btn-medium' %>
<%# </div> %>

<div class="new-container">
  <div class="table">
    <h2 class="heading"> Suas Doações </h2>
      <div class="block">
      <% @donations.each do |donation| %>
      <p><%= donation.medication.commercial_name %> - <%= donation.created_at.strftime('%d/%m/%y') %>
        <span class="price"><%= donation.units %>
          <sub>comprimidos</sub>
        </span>
        <ul class="options">
          <li><%= donation.medication.active_substance %> - <%= donation.medication.concentration %></li>
          <%# <li><%= donation.created_at.strftime('%d/%m/%y') %>
        </ul>
      </p>
      <% end %>
      </div>
      <div class="d-flex justify-content-center border-top-0 align-items-center">
        <button class="btn-green mb-5 ">
          <strong>Faça Outra Doação</strong>
        </button>
      </div>
  </div>
</div>
